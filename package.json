{
  "name": "claudehax-marketplace",
  "version": "0.2.0",
  "private": true,
  "description": "Claude Code marketplace/plugin for HAX CLI and HAX web components.",
  "scripts": {
    "refresh:cli": "bash scripts/generate-hax-cli-reference.sh",
    "refresh:elements": "bash scripts/generate-hax-elements-reference.sh",
    "refresh:docs": "bash scripts/refresh-docs.sh",
    "validate": "claude plugin validate ."
  }
}
